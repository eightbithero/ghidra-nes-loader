# Ghidra version this extension targets
ghidra.version=11.4.2

# Extension version (X.Y.Z). The patch component is bumped automatically
# by build.gradle each time the "buildExtension" task runs.
version=1.0.8
